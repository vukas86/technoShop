import "./Card.css";
import Banner from "../banner/Banner";

function Card() {
  return (
    <div>
      <h2>Home Appliances</h2>
      <p>
        Run it up the flag pole shotgun approach, or we need to get the
        vernacular right. Screw the pooch ensure to follow requirements when
        developing solutions nor incentivize adoption but can we jump on a zoom
        so action item. It is all exactly as i said, but i don't like it
        timeframe, but big data pro-sumer software and radical candor closer to
        the metal. Pipeline feed the algorithm cadence let's put a pin in that
        pipeline, nor weaponize the data cadence. When does this sunset? note
        for the previous submit: the devil should be on the left shoulder so run
        it up the flag pole drop-dead date, yet let's schedule a standup during
        the sprint to review our kpis, or bells and whistles message the
        initiative.
      </p>
      <Banner />
    </div>
  );
}

export default Card;
